streamlit==1.27.0
openai==1.0.0
llama-cpp-python==0.1.49
gemini-ai==0.0.1  # Placeholder, replace with the actual package name if it exists
requests==2.31.0  # For fetching FAQ content from URLs
Pillow==10.0.0  # For image processing
